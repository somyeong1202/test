using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hiriluk : MonoBehaviour
{
    //public float amount = 0.05f;   // 작아지는 비율
    //public float frameRate = 6f;  // 1초에 몇 번 반복할지

    //private Vector3 originalScale;
    //private float timer;
    //private bool shrink = true;

    //void Start()
    //{
    //    originalScale = transform.localScale;
    //}

    //void Update()
    //{
    //    timer += Time.deltaTime;
    //    if (timer >= 1f / frameRate)
    //    {
    //        timer = 0f;
    //        transform.localScale = shrink ? originalScale * (1 - amount) : originalScale;
    //        shrink = !shrink;
    //    }
    //}
}
